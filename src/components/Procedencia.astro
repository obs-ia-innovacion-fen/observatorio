---
const { tipo, revisado, fuente } = Astro.props;

const etiquetas = {
  propio: 'Investigación del Observatorio',
  colaboracion: 'Colaboración',
  comentada: 'Comentada',
  sugerencia: 'Sugerencia de lectura',
};

const revision =
  revisado === true ? 'revisado por pares'
  : revisado === false ? 'sin revisión por pares'
  : null;

const lectura =
  fuente === 'resumen' ? 'leído desde el resumen'
  : fuente === 'texto completo' ? 'leído en texto completo'
  : fuente ? `leído desde ${fuente}`
  : null;
---

<span class="procedencia" data-tipo={tipo}>{etiquetas[tipo] ?? tipo}</span>
{revision && <span class="revision">{revision}</span>}
{lectura && <span class="revision">{lectura}</span>}

<style>
  .revision {
    font-family: var(--dato);
    font-size: 0.7rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--gris);
    margin-left: 0.6rem;
  }
</style>
